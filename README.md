# SQL-Basics
SQL Basics Summary CREATE → make DB/tables  INSERT → add data  SELECT → get data  WHERE → filter  ORDER BY → sort  PRIMARY KEY → unique row  FOREIGN KEY → link tables  Constraints → keep data valid  Joins → combine tables  Dates/Numbers → easy to sort &amp; filter
